<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:dubbo="http://code.alibabatech.com/schema/dubbo"
       xsi:schemaLocation="http://www.springframework.org/schema/beans
       http://www.springframework.org/schema/beans/spring-beans.xsd
       http://code.alibabatech.com/schema/dubbo
       http://code.alibabatech.com/schema/dubbo/dubbo.xsd">
 
    <!-- 提供方应用信息，用于计算依赖关系 -->
    <dubbo:application name="${dubbo.app.name}"  />
  
    <!-- 使用multicast广播注册中心暴露服务地址 -->
<!--     <dubbo:registry address="multicast://224.5.6.7:1234" /> -->
    
    <!-- 使用zookeeper注册中心暴露服务地址 -->  
    <dubbo:registry id="reg1" address="${dubbo.registry.address}" register="${dubbo.register.flag}" group="${dubbo.register.group}"/>  
	<dubbo:registry id="reg2" address="${dubbo.registry.address2}" register="${dubbo.register.flag}" group="${dubbo.register.group}"/>   
	
	<!-- 用dubbo协议在20880端口暴露服务 -->
	<dubbo:protocol  id="dubbo" name="dubbo" port="${dubbo.app.port}" />
	<dubbo:provider protocol="dubbo" default="true" version="${dubbo.default.version}" timeout="350000" />
	
    <dubbo:monitor address="${dubbo.monitor.address}"/>
 
	
	<dubbo:service interface="com.yh.svc.gov.test.springboot1.manager.ApiService" ref="apiService" />
	<dubbo:service interface="com.yh.svc.gov.test.springboot1.manager.MetricsService" ref="metricsService" />
	 
</beans>
